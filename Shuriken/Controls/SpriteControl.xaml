<UserControl x:Class="Shuriken.Controls.SpriteControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Shuriken.Controls"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:viewmodels="clr-namespace:Shuriken.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:SpriteViewModel}"
             d:DesignHeight="200" d:DesignWidth="300"  BorderBrush="{StaticResource BorderBrush}" BorderThickness="2">
    <Grid>
        <ScrollViewer Margin="5">
            <ScrollViewer.Resources>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="Margin" Value="0 10 0 5"/>
                    <Setter Property="Padding" Value="5 0"/>
                </Style>
                <Style TargetType="{x:Type Grid}">
                    <Setter Property="Margin" Value="0 2"/>
                </Style>
            </ScrollViewer.Resources>
            <StackPanel>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="X" Grid.Column="0"/>
                    <hc:NumericUpDown Value="{Binding X}" Grid.Column="1"/>
                    <TextBlock Text="Y" Grid.Column="2"/>
                    <hc:NumericUpDown Value="{Binding Y}" Grid.Column="3"/>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Width" Grid.Column="0"/>
                    <hc:NumericUpDown Value="{Binding Width}" Grid.Column="1"/>
                    <TextBlock Text="Height" Grid.Column="2"/>
                    <hc:NumericUpDown Value="{Binding Height}" Grid.Column="3"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
